export enum WorkFlowStatus {
  PENDING = 'pending',
  RUNNING = 'running',
  COMPLETED = 'completed',
  FAILED = 'failed'
}

export enum TrackingType {
  BY_ROBOT = 'by-robot',
  BY_STAFF = 'by-staff'
}